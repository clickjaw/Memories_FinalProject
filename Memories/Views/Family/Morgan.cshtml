@*@model IEnumerable<Family>

<div class="container">
    <div class="row">
        @foreach (var item in Model)
        {
            <div class="col-md-4">
                <div class="card mb-4 box-shadow">
                    <div class="card-body">
                        <img class="FamilyPhoto" src="@item.FamilyMember.MemberImage" />
                        <a type="btn btn-success" asp-controller="FamilyMember" asp-action="Detail" asp-route-id="@item.FamilyCategory class="btn btn-sm btn-outline-secondary">@item.FamilyMember.FirstName @item.FamilyMember.LastName </a>
                    </div>
                </div>
            </div>
        }

    </div>
</div>*@








